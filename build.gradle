// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        minSdkVersion = 21
        targetSdkVersion = 28
        compileSdkVersion = 28

        supportLibVersion = '27.1.1'

        retrofitLibVersion = '2.4.0'
        glideLibVersion = '4.9.0'
        kotlinVersion = '1.3.50'
        coroutineVersion = '1.3.0-M1'
        retrofitVersion = '2.5.0' // version 2.6.0 WARNING!! supports non-null response body types
        roomVersion = '2.1.0'
        roomCoroutines_version = '2.1.0-alpha04'
        lifecycleVersion = '2.2.0-alpha04'
    }

    repositories {
        google()
        jcenter()

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files

        classpath 'com.github.ben-manes:gradle-versions-plugin:0.22.0'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
